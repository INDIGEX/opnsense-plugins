# The netbird port's own rc.d script only supports a single instance.
# This plugin drives every configured instance through its own
# os-netbird rc.d script instead (see the osnetbird template), so the
# port's script is always kept disabled here.
netbird_enable="NO"
